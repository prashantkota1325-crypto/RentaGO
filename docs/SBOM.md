# RentaGO Software Bill of Materials

Direct Python dependencies are listed in `requirements.txt` with pinned versions where available. A machine-generated lockfile/SBOM was not present during this audit.

## Direct Dependencies

```text
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-oracledb==2.5.1
openpyxl==3.1.5
python-multipart==0.0.20
jinja2==3.1.5
passlib==1.7.4
itsdangerous==2.2.0
python-dotenv==1.0.1
tzdata>=2024.1
```

Transitive dependency inventory and license metadata should be generated in a controlled build environment with `pip inspect` or an approved SBOM tool. Do not upload source or secrets to an unapproved third-party service.
